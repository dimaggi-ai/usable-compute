# Source application and unresolved physical claims

The September 20 sweep informs explicit contracts. Course descriptions and
release notes do not become measured capacity or execution permission.

| Source issue | Implemented disposition | Evidence still required |
|---|---|---|
| GB/GiB and runtime-device/chip ambiguity | Exact unit normalization; no guessed conversion; TPU Monitoring remains byte-valued runtime-device evidence until an explicit one-to-one inventory mapping is supplied | Actual device allocation and physical/shared-memory mapping; conflicting prose does not resolve itself |
| TPU generation, runtime, framework and topology changes | Versioned complete stack tuples, expiry, source pins, quarantine and source-watch impact mappings | Exact generation/runtime/framework/topology validation on hardware; no invented TPUv10 profile |
| Training, inference and agentic workload differences | Shared contract with distinct archetype, precision, per-device memory, latency, recovery and resource requirements | Representative workload measurements; a CPU smoke run does not supply accelerator SLOs |
| Pod size versus slice or regional availability | Scope-specific inventories and explicit budgets; nominal product capacity is not free scheduler capacity | Available inventory and allocation in the actual region, slice and namespace |
| GPU/Network Operator dependency conflicts | Exact stack identity must match a validated profile. Release changes queue review; separate nominally supported versions cannot imply joint compatibility | Joint manifests, CRDs, device plugins, NFD and driver/network tests on the selected stack |
| NVIDIA SMI unsupported fields and MIG | Missing values stay unknown; fixed-query normalization; unresolved MIG refuses whole-GPU admission | Supported per-instance inventory/telemetry adapter and physical validation before MIG execution |
| Published ceilings versus measured demand | Unknown headroom cannot satisfy positive demand. Explicitly omitted constraints remain visible | Actual power, bandwidth, thermal, checkpoint and tail-latency evidence where required |
| Dynamic documentation markup | Retain exact raw bytes; conservative change alerts; pinned no-material-change reviews preserve source timestamps | Each changed source is reviewed before affected profiles are eligible; no fuzzy automatic promotion |

The updated source register records hashes and application intent. Operational
sources include [TPU monitoring](https://docs.cloud.google.com/tpu/docs/monitor-tpu),
[Monitoring series collection](https://docs.cloud.google.com/monitoring/api/ref_v3/rest/v3/projects.timeSeries/list),
and [NVIDIA SMI](https://docs.nvidia.com/deploy/nvidia-smi/).
Authenticated Google Skills exports add concrete material on architecture,
deployment, storage, networking, accelerators and inference. Publisher transcripts
are retained as supplied; transcription errors are not numeric specification evidence.
No quiz answer was submitted and no Google Skills lab was started.

The research archive remains in the local reference library, separate from this
public repository. Course and video coverage is recorded there with unavailable
or stalled captions explicitly identified. This repository publishes source links,
hashes, implementation choices and synthetic fixtures, not account data, signed
download links or copied course packages.

Editorial review: passed — source distinctions, scope, unknowns and hardware
boundaries checked against the implementation and retained collection records.
