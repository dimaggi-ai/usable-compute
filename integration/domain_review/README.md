# Independent domain review runner

Runs the original pinned joined model, then explicitly substitutes only the
reviewed working cooling `Hall`, `Feeder`, `Start`, `admit` and `rung` entry points.
Six selected variants × seeds 0, 1 and 2 must preserve the complete finite output.
The four pinned NaN admissions must reproduce, while corrected constructors/API
must reject them. A separate process runs the owning scheduler coverage cases.

This is compatibility evidence for a domain correction. It does not accept the
application/authority receiver interface, run a workload or establish operator
performance. Original pinned code and historical evidence remain unchanged.

From the DIMAGGI AI workspace root, use a new or empty output directory:

```sh
projects/infrastructure/_Audits/2026-09-17-repo-aware-strategy/.venv/bin/python \
  projects/infrastructure/DIMAGGI-Infrastructure-Intelligence/research/integration/domain_review/review.py \
  --workspace-root "$PWD" --out /tmp/maggie-domain-review-new
```

The existing audit environment supplies dependencies; no network or installation
is used. `provenance.json` binds pinned input hashes and working source bytes;
HEAD alone is insufficient before local commits. Preserve the two output ledgers
and all source classes. The reference uses a private package namespace so the
working cooling package cannot contaminate the pinned baseline import.
